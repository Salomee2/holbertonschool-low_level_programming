#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: l'entier dont on souhaite connaitre la valeur
 * Return: Valeur absolue de n
 */
int _abs(int n)
{
if (n < 0)
{
	return (-n);
}
else
{
	return (n);
}
}
