#include <stdio.h>

/**
 * main - Affiche le nom du programme
 * @argc: Nombre d'arguments
 * @argv: Tableau des arguments passés au programm
 * Return: 0 si tout se passe bien
 */
int main(int argc, char *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
